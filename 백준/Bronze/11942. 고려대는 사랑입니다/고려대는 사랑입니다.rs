use std::io::{BufWriter, BufReader, BufRead, Write, stdin, stdout};
use std::cmp;

fn main() {
    let mut reader = BufReader::new(stdin().lock());
    let mut writer = BufWriter::new(stdout().lock());
 
   

      writeln!(writer,"{}","고려대학교").unwrap();

    

    
}
